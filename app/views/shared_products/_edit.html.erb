<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3">Edit <%= product_model %></h1>
      <%= link_to 'Cancel', polymorphic_path(product_model.constantize), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
      <%= render partial: 'shared_products/form', locals: { product: product } %>
    </div>
  </div>
</div>
