<div class="container mx-auto my-8 px-4">
  <div class="flex justify-between items-center mb-4">
    <h1 class="h3">Body Part Groups</h1>

    <% if @body_part_groups.any? %>
      <%= link_to 'New Category', new_category_path, class: "btn btn-primary" %>
    <% end %>
  </div>

  <% if @body_part_groups.any? %>
    <div class="bg-white rounded shadow table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @body_part_groups, :name, 'Name', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @body_part_groups, :subcategory_count, 'Subcategory count', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs text-gray-700 w-20">Actions</th>
          </tr>
        </thead>

        <tbody>
        <% @body_part_groups.each do |body_part_group| %>
          <tr class="group border-t border-gray-400 hover:bg-gray-100">
                      <td class="p-3"><%= body_part_group.name %></td>
                      <td class="p-3"><%#= category.subcategory_count %></td>

            <td>
              <%#= link_to "View", body_part_group, class: "btn btn-link no-underline" %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>

    <% if @pagy_body_part_groups.present? && @pagy_body_part_groups.pages > 1 %>
      <div class="text-center my-6">
        <%== pagy_nav(@pagy_body_part_groups) %>
      </div>
    <% end %>

  <% else %>
    <div class="bg-white rounded shadow flex flex-col items-center justify-between p-8 lg:flex-row">
      <%= image_tag "empty.svg", class: "mb-4 lg:w-1/2" %>
      <div class="flex-1 text-center">
        <p class="h3 mb-4">Create your first Category</p>
        <%= link_to 'New Category', new_category_path, class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
