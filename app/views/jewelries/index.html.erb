<div class="container mx-auto my-8 px-4">
  <div class="flex justify-between items-center mb-4">
    <h1 class="h3">Jewelries</h1>

    <% if @jewelries.any? %>
      <%= link_to 'New Jewelry', new_jewelry_path, class: "btn btn-primary" %>
    <% end %>
  </div>

  <% if @jewelries.any? %>
    <div class="bg-white rounded shadow table-responsive">
      <table class="table">
        <thead>
          <tr>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :user_id, 'User', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :brand_id, 'Brand', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :name, 'Name', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :product_image_data, 'Product image data', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :description, 'Description', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :retail_price, 'Retail price', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :type_of, 'Type of', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :gender, 'Gender', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :material, 'Material', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :primary_color, 'Primary color', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :secondary_color, 'Secondary color', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs"><%= sortable @jewelries, :product_url, 'Product url', class: 'text-gray-700' %></th>
            <th class="p-3 uppercase text-left text-xs text-gray-700 w-20">Actions</th>
          </tr>
        </thead>

        <tbody>
        <% @jewelries.each do |jewelry| %>
          <tr class="group border-t border-gray-400 hover:bg-gray-100">
                      <td class="p-3"><%= jewelry.user_id %></td>
                      <td class="p-3"><%= jewelry.brand_id %></td>
                      <td class="p-3"><%= jewelry.name %></td>
                      <td class="p-3"><%= jewelry.product_image_data %></td>
                      <td class="p-3"><%= jewelry.description %></td>
                      <td class="p-3"><%= jewelry.retail_price %></td>
                      <td class="p-3"><%= jewelry.type_of %></td>
                      <td class="p-3"><%= jewelry.gender %></td>
                      <td class="p-3"><%= jewelry.material %></td>
                      <td class="p-3"><%= jewelry.primary_color %></td>
                      <td class="p-3"><%= jewelry.secondary_color %></td>
                      <td class="p-3"><%= jewelry.product_url %></td>
          
            <td>
              <%= link_to "View", jewelry, class: "btn btn-link no-underline" %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>

    <% if @pagy.pages > 1 %>
      <div class="text-center my-6">
        <%== pagy_nav(@pagy) %>
      </div>
    <% end %>

  <% else %>
    <div class="bg-white rounded shadow flex flex-col items-center justify-between p-8 lg:flex-row">
      <%= image_tag "empty.svg", class: "mb-4 lg:w-1/2" %>
      <div class="flex-1 text-center">
        <p class="h3 mb-4">Create your first Jewelry</p>
        <%= link_to 'New Jewelry', new_jewelry_path, class: "btn btn-primary" %>
      </div>
    </div>
  <% end %>
</div>
